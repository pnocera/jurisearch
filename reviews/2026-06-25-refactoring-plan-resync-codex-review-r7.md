## BLOCKER

- `work/06-refactoring/refactoring-plan.md:185`-`204` still leaves shared eval/retrieval query helpers unassigned. `PreparedQueryEmbedder` is defined at `crates/jurisearch-cli/src/main.rs:3528` and is used by eval paths (`france_legi_search_documents` at `main.rs:2423`, `france_juris_retrieval_category` at `main.rs:2589`, `france_juris_search_documents` at `main.rs:2619`, `france_juris_zone_retrieval_category` at `main.rs:2900`, `france_juris_zone_search_documents` at `main.rs:2934`) and retrieval paths (`search_with_postgres` at `main.rs:3659`, plus `compare_payload` builds the same embedder at `main.rs:3921`). `parade_query_text` is defined at `main.rs:11879` and is also shared by eval (`eval_run_payload`, France-LEGI, France-juris, France-juris-zones) and retrieval (`search_payload`, `zone_search_payload`, `compare_payload`). `validate_retrieval_options` is defined at `main.rs:420` and is called by both `eval_run_payload` (`main.rs:1797`) and `search_payload` (`main.rs:3283`). Phase 3a moves the retrieval callers before the eval split, but the plan does not say where these eval-facing helpers live, so the split would either leave them in `main.rs` or force an unplanned dependency. Concrete fix: add an explicit Phase 3a prerequisite/assignment for these helpers. The lowest-risk wording is to move `PreparedQueryEmbedder` into `embedding_runtime.rs` when `search_with_postgres` moves, and move `parade_query_text` + `validate_retrieval_options` into a tiny shared retrieval-support module (or explicitly make them `pub(crate)` in `retrieval.rs` and say `eval.rs` may import them as part of the production retrieval path).

- `work/06-refactoring/refactoring-plan.md:197` / `work/06-refactoring/refactoring-plan.md:288` still leaves the real ISO-date validator split across proposed modules. `validate_as_of` is retrieval-local (`cite_payload` at `main.rs:5180`, `context_payload` at `main.rs:5266`), but it calls `is_valid_iso_date` at `main.rs:11814`; `is_valid_iso_date` is also called by `diff_payload` at `main.rs:9824`, which Phase 4 moves to `status.rs`, and it depends on `days_in_month`. This is not the same helper as the simpler `is_iso_date` assigned to citation routing at `main.rs:3608`; it performs calendar validation. Concrete fix: add a shared `date.rs`/`validation.rs` assignment for `is_valid_iso_date` + `days_in_month`, or explicitly place them in a shared runtime/args module imported by both `retrieval.rs` and `status.rs`.

## WARN

- The r6 `rfind_ascii_ci` blocker is resolved. The plan now adds `ascii.rs` to the module map at `work/06-refactoring/refactoring-plan.md:66` and explicitly assigns `find_ascii_ci` + `rfind_ascii_ci` at `work/06-refactoring/refactoring-plan.md:200`. That matches current source: `find_ascii_ci` is defined at `main.rs:3622` and called by `legi_citation_routing`; `rfind_ascii_ci` is defined at `main.rs:3636` and called by both `legi_citation_routing` (`main.rs:3594`, retrieval) and `heuristic_dispositif` (`main.rs:5150`, enrichment/decision_part). No fix needed for this item.

- The newly named single-module helper placements are source-accurate. `find_ci` is defined at `main.rs:4422` and is only called by `extract_first_href` (`main.rs:4431`) and `split_article_code` (`main.rs:4455`), both assigned to `enrichment/legislation.rs`. `env_i64` is defined at `main.rs:5086` and is only used by Judilibre-zone TTL handling (`enrich_decision_from_judilibre_with_client` / `cache_zone_status_with_client`), matching `enrichment/judilibre_zones.rs`. `ZoneEnrichOutcome` is defined at `main.rs:7283` and is only used by `enrich_zone_page_concurrently` and `worker_outcomes_or_errors`, matching `ingest.rs`. No fix needed for this item.

## NIT

- `work/06-refactoring/refactoring-plan.md:91` and `work/06-refactoring/refactoring-plan.md:286` mention shared runtime/config/output helpers and shared error mapping, but the module map does not name a home for broad `ErrorObject` helper constructors such as `dependency_unavailable` (`main.rs:11243`) and `no_results` (`main.rs:11253`), which are used across eval, retrieval, enrichment, ingest, embedding, and status paths. This is less risky than the blockers above because the plan already preserves the `ErrorObject` boundary, but an explicit `errors.rs`/`error_helpers.rs` line would make the move checklist more mechanical. Concrete fix: add a small shared error-helper module to the module map, or say these helpers stay with `output.rs`/`index_runtime.rs` and are imported by command modules.

VERDICT: FIXES_REQUIRED
