# BLOCKER

1. `legifrance_code_search_body` is still a cross-command helper, but the plan assigns it to `enrichment/legislation.rs` instead of a shared leaf. The plan moves `cite_payload` into `retrieval.rs` in Phase 3a (`work/06-refactoring/refactoring-plan.md:208`) and later assigns `legifrance_code_search_body`/`sanitize_legifrance_query` to `enrichment/legislation.rs` (`work/06-refactoring/refactoring-plan.md:259`, `work/06-refactoring/refactoring-plan.md:526`). In source, `cite_payload` owns the `cite --online` path (`crates/jurisearch-cli/src/main.rs:5180`) and calls `apply_online_citation_confirmation`, which calls `legifrance_code_search_body` (`crates/jurisearch-cli/src/main.rs:11707`). The same request-body helper is used by `enrich_legislation_citations_payload` (`crates/jurisearch-cli/src/main.rs:4776`). As written, the implementer either leaves the helper behind in `main.rs` during the Phase 3a retrieval move or makes `retrieval.rs` import a sibling command module (`enrichment/legislation.rs`) just to reach a low-level Legifrance request-body builder, which contradicts the new shared-leaf principle (`work/06-refactoring/refactoring-plan.md:102`) and the dependency direction (`work/06-refactoring/refactoring-plan.md:94`). Fix: add a shared leaf such as `legifrance_search.rs`/`legislation_support.rs` containing `LEGIFRANCE_QUERY_MAX_CHARS`, `sanitize_legifrance_query`, `legifrance_code_search_body`, and their tests; have both `retrieval.rs` (`apply_online_citation_confirmation`) and `enrichment/legislation.rs` import it. Keep `legifrance_response_has_results` in `enrichment/legislation.rs` if it remains enrichment-only. Update the module map, shared-leaf list, Phase 3a prerequisite list, Phase 3b move list, suggested commit 4, and the official-api secondary-refactor note accordingly.

# WARN

1. `today_utc` is another pure helper used across eval and retrieval but is not called out in the concrete leaf list. It is used by eval paths (`crates/jurisearch-cli/src/main.rs:1843`, `crates/jurisearch-cli/src/main.rs:2264`, `crates/jurisearch-cli/src/main.rs:2962`) and retrieval paths (`crates/jurisearch-cli/src/main.rs:3414`, `crates/jurisearch-cli/src/main.rs:3682`, `crates/jurisearch-cli/src/main.rs:3910`, `crates/jurisearch-cli/src/main.rs:5183`). The general principle covers it, so this is not a blocker, but adding `today_utc` to `date.rs` in the leaf list would prevent the next implementation pass from rediscovering it mid-move. Fix: mention `today_utc` under `date.rs` or create a tiny time/date support note in Phase 3a.

2. The plan still has a placement ambiguity for error mapping. The shared-leaf section says `errors.rs` owns `storage_error_object`, `embedding_error_object`, and `embedding_error_object_with_context` (`work/06-refactoring/refactoring-plan.md:108`), but Phase 4 still says `index_runtime.rs` may take "storage/embedding error mapping if it is shared across command modules" (`work/06-refactoring/refactoring-plan.md:307`). Those helpers are shared across eval, retrieval, status, ingest, enrichment, and embedding paths, so the later Phase 4 bullet contradicts the earlier concrete assignment. Fix: remove the Phase 4 error-mapping bullet or rewrite it to say `index_runtime.rs` imports `errors.rs`; do not make `index_runtime.rs` the owner.

# NIT

None.

VERDICT: FIXES_REQUIRED
