## BLOCKER

- `work/06-refactoring/refactoring-plan.md:189` / `work/06-refactoring/refactoring-plan.md:223` still leaves `rfind_ascii_ci` unassigned even though it is used from two proposed target modules. In current source, `legi_citation_routing` calls `rfind_ascii_ci` at `crates/jurisearch-cli/src/main.rs:3594`; that routing/citation path is part of the retrieval/eval citation split. The decision-part enrichment path also calls the same helper from `heuristic_dispositif` at `crates/jurisearch-cli/src/main.rs:5150`, which Phase 3b moves to `enrichment/decision_part.rs`. After the split, either `citation.rs` and `enrichment/decision_part.rs` would need a sibling dependency, or one module would have to reach back into `main.rs` for the helper. Concrete fix: add `rfind_ascii_ci` to an explicitly shared helper module used by both paths, or put it in `citation.rs` only if the plan also says `enrichment/decision_part.rs` may depend on that parser utility module. The lower-risk wording is to create a tiny shared text/ascii helper module (`text.rs` or `ascii.rs`) and move both `find_ascii_ci` and `rfind_ascii_ci` there, because `rfind_ascii_ci`'s doc comment references `find_ascii_ci`.

## WARN

- The r5 blocker is resolved. The new `enrichment/archive.rs` assignment is source-accurate: `archive_exchange` is defined at `crates/jurisearch-cli/src/main.rs:4332`, is called by `enrich_legislation_citations_payload` at `crates/jurisearch-cli/src/main.rs:4778`, and is called by `enrich_decision_from_judilibre_with_client` for Judilibre `/search` and `/decision` at `crates/jurisearch-cli/src/main.rs:4892` and `crates/jurisearch-cli/src/main.rs:4908`. `sha256_hex` is defined at `crates/jurisearch-cli/src/main.rs:4321` and is used by both `archive_exchange` at `crates/jurisearch-cli/src/main.rs:4341` and `archive_local_unsupported` at `crates/jurisearch-cli/src/main.rs:4398`; `archive_local_unsupported` is Judilibre-only and is called at `crates/jurisearch-cli/src/main.rs:4878`. Keeping those three in `enrichment/archive.rs` removes the cross-submodule archive dependency between `enrichment/legislation.rs` and `enrichment/judilibre_zones.rs`.

- I did not find another unassigned helper shared by two or more of the proposed enrichment / Phase 5 target modules beyond `rfind_ascii_ci`. The other omitted local helpers I saw are single-module dependencies: `find_ci` at `crates/jurisearch-cli/src/main.rs:4422` is used by `extract_first_href` and `split_article_code`, both assigned to `enrichment/legislation.rs`; `env_i64` at `crates/jurisearch-cli/src/main.rs:5086` is only used by Judilibre-zone cache TTL handling; and `ZoneEnrichOutcome` at `crates/jurisearch-cli/src/main.rs:7283` is local to the `ingest enrich-zones` helpers. These can move with their owning module without creating a sibling dependency.

## NIT

- Consider naming those single-module dependencies explicitly in the plan while touching the helper lists: move `find_ci` with `enrichment/legislation.rs`, move `env_i64` with `enrichment/judilibre_zones.rs`, and move `ZoneEnrichOutcome` with `ingest.rs`. This is not the same cross-module blocker as `rfind_ascii_ci`, but it would make the mechanical move checklist less error-prone.

VERDICT: FIXES_REQUIRED
