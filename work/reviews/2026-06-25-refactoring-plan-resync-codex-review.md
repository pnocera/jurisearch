# Code Review: Refactoring Plan Resync

## BLOCKER

- `work/06-refactoring/refactoring-plan.md:172-204`, `work/06-refactoring/refactoring-plan.md:498-515`, and `work/06-refactoring/refactoring-plan.md:517-522` give contradictory ordering for the retrieval/enrichment split. The phase list and commit plan move retrieval first (`Phase 3`, commit 4) and extract enrichment second (`Phase 3b`, commit 5), but the caveat/risk text says the enrichment extraction must happen before the retrieval and ingest moves. The source supports the latter ordering: `fetch_payload` at `crates/jurisearch-cli/src/main.rs:4056` calls `annotate_fetched_parts`, which calls `official_decision_part`, which can call the same `enrich_decision_from_judilibre` / `enrich_decision_from_judilibre_with_client` path used by `enrich_zones_payload` at `crates/jurisearch-cli/src/main.rs:7297`. Applying the plan as written would either drag the decision-zone enrichment band into `retrieval.rs` temporarily or force another large cross-module move immediately afterward.
  - Recommended fix: reorder the plan so the shared decision-part/Judilibre zone extraction happens before moving `fetch_payload` into `retrieval.rs`. Either make the current Phase 3b the new Phase 3 and move retrieval afterward, or split retrieval into `3a` for search/compare/cite/context/related/expand, `3b` for enrichment, and `3c` for `fetch_payload`. Update the suggested commit plan and the risk-area wording to match the chosen order.

## WARN

- `work/06-refactoring/refactoring-plan.md:83-94` and `work/06-refactoring/refactoring-plan.md:521-522` overstate the shared dependency rationale for the whole `enrichment/` subtree. Decision-part fetch and Judilibre zone backfill do share `decision_zones` (`official_decision_part` reads `decision_zones_json`; `enrich_decision_from_judilibre_with_client` writes `upsert_decision_zones_with_client`). Legislation citation collection/resolution does not use `decision_zones`: `collect_legislation_citations_payload` reads archived Judilibre `/decision` responses and writes `decision_legislation_citations` / `legislation_citation_resolutions`, while `enrich_legislation_citations_payload` archives Legifrance responses in `official_api_responses` and updates `legislation_citation_resolutions`.
  - Recommended fix: narrow the dependency description. Say the decision-part/Judilibre-zone modules share the `decision_zones` cache and official API archive, while `legislation.rs` shares the official API client/archive and the `legislation_citations` storage tables. Keep the `enrichment/` grouping if desired, but base it on official-API enrichment orchestration rather than claiming all three paths share `decision_zones`.

- `work/06-refactoring/refactoring-plan.md:234-241` and `work/06-refactoring/refactoring-plan.md:310-314` misclassify the zone-unit embedding helper. Phase 4 says embedding pool config/worker helpers move to `embedding_runtime.rs`, but Phase 5 says `embed_and_insert_zone_units_with_pool` should move to `ingest.rs`. In source, `embed_and_insert_zone_units_with_pool` at `crates/jurisearch-cli/src/main.rs:8229` is the zone-unit twin of `embed_and_insert_chunks_with_pool` and both are thin wrappers over the generic `embed_and_insert_with_pool` driver. Splitting one wrapper into ingest while moving the pool driver/runtime elsewhere will either create a backward dependency from embedding runtime to ingest or scatter identical embedding concerns.
  - Recommended fix: move `embed_and_insert_with_pool`, `embed_and_insert_chunks_with_pool`, `embed_and_insert_zone_units_with_pool`, and the associated pool structs into `embedding_runtime.rs` (or a dedicated embedding pipeline module). Keep `embed_chunks_payload` and `embed_zone_units_payload` in `ingest.rs` as orchestration that calls those helpers. Update the Phase 5 bullet to list only `embed_zone_units_payload` under ingest.

- `work/06-refactoring/refactoring-plan.md:35` and `work/06-refactoring/refactoring-plan.md:525` say the working tree is clean as of 2026-06-25, but the current review scope itself is one uncommitted modification: `work/06-refactoring/refactoring-plan.md` is modified. The underlying source tree may be clean apart from the plan document, but the current wording is false in this working tree and could confuse the "start from clean HEAD" instruction.
  - Recommended fix: change both statements to something like "Apart from this plan document, the implementation/source tree is clean; once this plan edit is committed, the refactor can start from a clean `HEAD`." Keep the reminder to re-confirm cleanliness before each mechanical move.

## NIT

- `work/06-refactoring/refactoring-plan.md:124` says `IngestSubcommand` has "now 11 variants". The current enum at `crates/jurisearch-cli/src/main.rs:943` has 10 variants: `PlanArchives`, `LegiArchives`, `JuriArchives`, `EmbedChunks`, `EnrichZones`, `BuildZoneUnits`, `EmbedZoneUnits`, `CollectLegislationCitations`, `EnrichLegislationCitations`, and `BackfillLegiHierarchy`.
  - Recommended fix: change "now 11 variants" to "now 10 variants" and keep the list of the five new enrichment/zone variants.

- `work/06-refactoring/refactoring-plan.md:330-332` still says `crates/jurisearch-cli/tests/cli_contract.rs` should be reduced from 4,954 lines. The current file is 5,049 lines, which already appears correctly in the size-profile table at `work/06-refactoring/refactoring-plan.md:23`.
  - Recommended fix: update the Phase 6 target line to 5,049 lines so the phase text matches the table and the current tree.

- `work/06-refactoring/refactoring-plan.md:459-470` labels the proposed `official-api` split as `legifrance.rs # Legifrance code-search request/response helpers`, but the code-search request body and response classification helpers currently live in the CLI (`legifrance_code_search_body`, `sanitize_legifrance_query`, `legifrance_response_has_results` at `crates/jurisearch-cli/src/main.rs:4569-4619`). The official API crate owns the generic Legifrance token/search exchange surface (`PisteClient::legifrance_search_exchange` at `crates/jurisearch-official-api/src/lib.rs:393`), not the citation-specific code-search JSON shaping.
  - Recommended fix: rename the split comment to "Legifrance OAuth/search exchange client" or explicitly say that `legifrance.rs` in `jurisearch-official-api` should keep only generic HTTP/token/exchange code, while citation-specific request body and response interpretation stay in the CLI `enrichment/legislation.rs` module unless a non-CLI caller appears.

VERDICT: FIXES_REQUIRED
