BLOCKER crates/jurisearch-cli/src/main.rs:654: `eval france-legi` accepts any `--top-k`, but `france_legi_artifact` always records the results under `*_at_10` threshold keys and the Phase A/B gate validates those fixed @10 fields. Running the benchmark with `--top-k 20` can therefore emit a `state: passed` artifact that the gate treats as recall/exactness@10 even though the runner measured @20. Fix: for this gate artifact, reject any value other than 10 before opening the index, or remove the option. If the runner must remain top-k configurable, the artifact schema and gate need to record and validate the actual cutoff and should not pass the Phase 1 @10 gate unless `top_k == 10`.

WARN crates/jurisearch-cli/src/main.rs:805: `sampled=false` is defensible only if the policy meaning is "not random/ad hoc/cherry-picked"; the runner still uses bounded `ORDER BY ... LIMIT` qrel extraction via `FranceLegiGoldLimits`. The artifact's `qrel_selection=deterministic_bounded_by_document_id` and evidence counts are honest enough for a deterministic bounded gate, but they conflict with the validator comment/error wording that says "no sampled/truncated qrels". Fix: clarify the gate policy wording or provenance by explicitly recording the per-category limits and that the qrel set is deterministic bounded, not complete. If the gate requires the full qrel universe, this runner should either remove the caps for gate artifacts or set a different provenance flag that the gate rejects.

NIT crates/jurisearch-cli/src/main.rs:606: `--out` writes with `fs::write` directly, so a missing parent directory fails even though the rest of the artifact generation succeeded. That is not a correctness problem if callers are expected to create the directory, and stdout still emits only after a successful write. Fix: either document that parents must exist, or call `create_dir_all(parent)` before `fs::write` for a friendlier operator path.

NIT crates/jurisearch-cli/src/main.rs:611: `serde_json::to_string_pretty(&response).unwrap_or_default()` would silently write an empty file if serialization ever failed. `serde_json::Value` serialization is effectively infallible here, so this is low risk, but the error path should still preserve the failure if this code is copied to a fallible payload later. Fix: map the serialization error into `dependency_unavailable` before attempting the file write.

The metric loops otherwise look correct: known-item and temporal count no-results as misses, document IDs are deduplicated before top-k truncation, empty categories resolve to zero instead of dividing by zero, and cross-reference computes mean per-query recall over gold source UIDs. The `france_legi_artifact` pass condition mirrors the validator's policy floors plus minimum query counts, and the unit test exercises the pure artifact-to-gate pass contract. Cross-reference `as_of` from the citing document date is a sound choice for "what was cite-resolving search able to see when the citing article existed"; if the desired claim is instead "can today's reader find the cited article regardless of the citing version date", that should be a separate metric rather than changing this one.
VERDICT: FIXES_REQUIRED
