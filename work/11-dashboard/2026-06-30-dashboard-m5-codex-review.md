WARN - Embedded mode can silently fall back to `DevAssetSource` when the generated manifest is the stub. `apps/dashboard/server/main.ts:40-48` treats `process.env.DASHBOARD_EMBEDDED` as "try embedded", but if `EMBEDDED_INDEX === null` it falls through to `new DevAssetSource(...)`; `apps/dashboard/build/gen-embed.ts:51-56` deliberately writes exactly that null-index stub when `web/dist` is absent, and `apps/dashboard/package.json:18-20` can create the stub during clean `typecheck`/`test` runs before any web build. The blessed `bun run compile` path currently rebuilds `web/dist` and regenerates the manifest in the right order, so the happy path is covered. The risk is that a release/integration path, or a manual `bun build server/main.ts --compile --define process.env.DASHBOARD_EMBEDDED=1`, can compile a binary against the stub and still pass `--version` because `run()` exits before resolving assets. At runtime that binary would start in embedded mode, fall back to `DevAssetSource`, and then fail to serve the SPA from Bun's virtual compiled filesystem instead of failing clearly at startup or build time.

Concrete fix: make embedded mode fail closed. In `resolveAssetSource()`, when `process.env.DASHBOARD_EMBEDDED` is truthy, throw a fatal error if `EMBEDDED_INDEX === null` instead of returning `DevAssetSource`. Keep the stub only as a typecheck/dev artifact for the non-embedded branch. For extra protection, make `build/compile.ts` call the generator in a required-dist mode that exits nonzero if `web/dist/index.html` is missing.

NIT - The embed smoke proves the current binary is standalone, but it does not prove manifest completeness. `apps/dashboard/server/embed-smoke.test.ts:163-195` fetches one arbitrary `.js`, one `.css`, and one `.woff2` from `web/dist/assets`; a regression that omitted a different lazy chunk, a nested asset, `/index.html`, or the emitted `.woff` fallbacks could still pass. The generator itself recursively walks `web/dist` and the current manifest includes those files, so this is a coverage gap rather than a current packaging failure.

Concrete fix: after `bun run compile`, enumerate every file under `web/dist`, convert each to `/${relativePath}`, and fetch each path from the isolated binary, asserting `200`, nonempty bytes for non-HTML assets, and expected content type classes. Include `/index.html` explicitly. Keep the existing missing-asset `404` assertion.

VERDICT: FIXES_REQUIRED
