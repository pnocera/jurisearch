## Findings

BLOCKER: The new ownership-gap comment incorrectly says a "normally-migrated DB" already has `public.chunk_embeddings` / `public.zone_unit_embeddings` owned by the jurisearch owner role, but the normal external provisioning path migrates as the admin before provisioning the owner/writer/read roles. `provision_external_db` connects as `target_admin` and runs `run_migrations_on` first (`crates/jurisearch-storage/src/provision.rs:91`-`97`), then calls `provision_producer_roles` only afterward (`crates/jurisearch-storage/src/provision.rs:99`-`102`). The provisioning SQL also still reassigns only app schemas plus `public.index_manifest`, `public.schema_migrations`, `public.package_change_log`, and `public.package_catalog` (`crates/jurisearch-storage/src/backend.rs:503`-`535`), not the dense DDL targets. The real finalize DDL creates indexes on `chunk_embeddings` and `zone_unit_embeddings` (`crates/jurisearch-storage/src/dense.rs:253`-`260`, `crates/jurisearch-storage/src/zone_units.rs:687`-`694`), so table ownership/owner-membership remains required there. As written, the comment and adjacent producer test comment imply this patch makes dense finalize succeed on a normal migrated external DB except for hand-loaded/external corpora (`crates/jurisearch-storage/src/backend.rs:708`-`716`, `crates/jurisearch-storage/src/backend.rs:971`-`974`), but the reviewed provisioning order leaves the same table-ownership failure possible on the blank `provision_external_db` path too unless a separate manual ownership reassignment has already happened. Fix by either transferring the producer DDL target tables to `{owner}` (or otherwise ensuring migrations create them as `{owner}`), or by narrowing the comments/tests to state that this patch only fixes the schema-`CREATE` half and that current externally provisioned DBs still require a separate ownership step before dense finalize.

## Notes

The r1 probe blocker is resolved in the narrow sense: `crates/jurisearch-storage/src/provision.rs` has no working-tree diff, and the `probe_role_can_create_in_public` / `can_create_in_public` names are absent from the reviewed source.

The rendered grant is producer-profile-only: the only shared CREATE-on-public revoke is `REVOKE CREATE ON SCHEMA public FROM PUBLIC;` (`crates/jurisearch-storage/src/backend.rs:493`-`495`), the site branch asserts no `GRANT CREATE ON SCHEMA public` (`crates/jurisearch-storage/src/backend.rs:884`-`890`), and the producer branch renders `GRANT CREATE ON SCHEMA public TO {owner};` (`crates/jurisearch-storage/src/backend.rs:697`-`717`). That grant is idempotent and is not stripped by the existing convergence revoke because the revoke targets the `PUBLIC` pseudo-role, not the explicit owner role.

The two backend SQL tests assert the intended string-level behavior: site omission and exact producer rendering (`GRANT CREATE ON SCHEMA public TO "jurisearch_owner";`). I did not rerun the full validation listed in the brief; I reviewed the source/diff and ran `git diff --check -- crates/jurisearch-storage/src/backend.rs`, which was clean.

VERDICT: FIXES_REQUIRED
