The idea is to have a hosted jurisearch service + postgresql server which responsability will be to ingest new legal data on a regular basis ( download / ingest / calculate embeddings, etc ) and to provide deltas to client machines. the data will be replicated ( or synchronized ) by the client applications every day. the jurisearch client application will still be a cli connected to a local database.