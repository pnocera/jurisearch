# Codex review r2 - target deployment analysis

## Findings

No findings.

## Verified Checks

- Fix 1 in section 3 is now accurate. The analysis says `jurisearch serve` runs the JSONL line protocol through `dispatch_session_request`, that this dispatcher is distinct from `dispatch::run`, and that matching results come from shared payload builders rather than a shared top-level handler (`work/09-jurisearch-cli/01-target-deployment-analysis.md:64-70`). The source matches that: `serve_jsonl` calls `dispatch_session_request` after optional `index_dir` injection (`crates/jurisearch-cli/src/serve.rs:99-103`); one-shot dispatch is `dispatch::run` (`crates/jurisearch-cli/src/dispatch.rs:15-71`); and the session wrappers call the same payload builders used by the one-shot emitters for the query surface (`crates/jurisearch-cli/src/session.rs:33-86`, `crates/jurisearch-cli/src/retrieval/search.rs:5-9`, `crates/jurisearch-cli/src/retrieval/search.rs:38-102`, `crates/jurisearch-cli/src/retrieval/cite.rs:5-13`).
- Fix 1 in section 6 is now accurate. The transport trade-off now says JSONL produces the same results as the CLI through shared payload builders (`work/09-jurisearch-cli/01-target-deployment-analysis.md:174-176`). That is the correct relationship for the reviewed claim: the top-level dispatchers differ, but the request payload builders are shared.
- Fix 1 in section 7 is now accurate. The reusable-surface bullet says the session dispatcher per-command wrappers reuse the same payload builders as the one-shot CLI (`work/09-jurisearch-cli/01-target-deployment-analysis.md:217-220`). `session_search_payload`, `session_fetch_payload`, `session_cite_payload`, `session_context_payload`, `session_related_payload`, and `session_compare_payload` deserialize session args and delegate to the corresponding shared builders (`crates/jurisearch-cli/src/session.rs:33-67`).
- Fix 2 in section 8 is now accurate. The risk now distinguishes hybrid mismatch behavior from explicit dense mismatch behavior (`work/09-jurisearch-cli/01-target-deployment-analysis.md:242-246`). In hybrid mode, SQL builds both a lexical CTE and a dense CTE; only the dense CTE filters `chunk_embeddings` by `embedding_fingerprint`, so a fingerprint mismatch can leave lexical-only results (`crates/jurisearch-storage/src/retrieval/sql.rs:92-175`). In explicit dense mode, the SQL has only the dense branch, applies the same fingerprint filter, and ranks only dense rows, so a mismatch can yield no dense matches / false no-results (`crates/jurisearch-storage/src/retrieval/sql.rs:205-253`). `RetrievalMode::uses_lexical` and `uses_dense` also support this distinction (`crates/jurisearch-storage/src/retrieval/types.rs:226-232`).
- The "main chunk search needs a fail-closed fingerprint preflight" framing is source-backed. Main search readiness checks projection/dense coverage but does not compare the query embedder fingerprint against the indexed fingerprint (`crates/jurisearch-cli/src/index_runtime.rs:97-136`); the search path then embeds the query and passes the fingerprint into retrieval SQL, where it is used as a filter (`crates/jurisearch-cli/src/retrieval/search.rs:190-197`, `crates/jurisearch-cli/src/retrieval/search.rs:301-319`). By contrast, zone search has an explicit preflight that fails if the indexed zone embedding fingerprint differs from the expected query fingerprint (`crates/jurisearch-cli/src/retrieval/zone.rs:5-47`).
- I found no new factual error or analysis-only scope creep in the edited locations. The document still frames the work as analysis, trade-offs, feasibility, and risks, and it explicitly defers target shape, schemas/APIs, and sequencing to the architecture/planning documents (`work/09-jurisearch-cli/01-target-deployment-analysis.md:260-265`).

VERDICT: GO
