# Review - Task 0 Contracts/API Survey r4

## Findings

No BLOCKER/WARN/NIT findings.

## Re-verification Notes

The r4 plan no longer depends on hand-enumerating every storage helper in prose. The storage-helper deliverable is now framed as a functional scope rule: M1-B owns every `ManagedPostgres`-typed or `execute_sql`-based storage helper reachable from the ingest -> enrich -> embed extraction path, the visible lists are explicitly illustrative/non-exhaustive, and the codegraph call-graph audit result is itself the deliverable M1-C depends on (`work/10-next-plans/task0-contracts-survey.md:101`, `work/10-next-plans/task0-contracts-survey.md:111`, `work/10-next-plans/task0-contracts-survey.md:118`). That is a sound resolution for the storage-helper-completeness risk because GO no longer rests on this planning document having a perfect static list.

The r3 missing tail is now represented both as examples and as part of the audited functional scope: `load_derivable_decision_zones_json`, `load_zone_unit_embedding_inputs`, `insert_zone_unit_embeddings`, `finalize_zone_dense_rebuild`, `insert_chunk_embeddings`, `invalidate_cached_query_readiness`, and `load_archived_decisions_with_visa_json` are called out in the revision note and helper sample (`work/10-next-plans/task0-contracts-survey.md:33`, `work/10-next-plans/task0-contracts-survey.md:99`, `work/10-next-plans/task0-contracts-survey.md:130`). The real source still matches the risk being planned around: for example, `load_derivable_decision_zones_json` uses `postgres.execute_sql` (`crates/jurisearch-storage/src/zone_units.rs:228`, `crates/jurisearch-storage/src/zone_units.rs:251`), `load_zone_unit_embedding_inputs` and `insert_zone_unit_embeddings` take `&ManagedPostgres` (`crates/jurisearch-storage/src/zone_units.rs:304`, `crates/jurisearch-storage/src/zone_units.rs:349`), `invalidate_cached_query_readiness` takes `&ManagedPostgres` (`crates/jurisearch-storage/src/ingest_accounting/readiness.rs:193`), and the LEGI ingest path still reaches `backfill_legi_article_hierarchy_from_metadata_scoped` plus replay refresh through `&postgres` (`crates/jurisearch-cli/src/ingest/legi.rs:245`, `crates/jurisearch-cli/src/ingest/legi.rs:299`).

The dependency edges now prevent M1-C from starting too early. The branch split table makes M1-C depend on `C0 + S1 from M1-B + M1-B complete storage-helper generalization (audited set, merged)` (`work/10-next-plans/task0-contracts-survey.md:191`). The shared-file hazard section repeats that M1-B's call-graph audit and full helper generalization must merge before M1-C begins and that M1-C never edits storage (`work/10-next-plans/task0-contracts-survey.md:201`, `work/10-next-plans/task0-contracts-survey.md:208`). The handoff protocol is also explicit: audit first, generalize the audited set, merge that deliverable before M1-C extracts S4-S6, then M1-C starts rebased on the trait plus generalized helpers (`work/10-next-plans/task0-contracts-survey.md:280`, `work/10-next-plans/task0-contracts-survey.md:289`, `work/10-next-plans/task0-contracts-survey.md:302`, `work/10-next-plans/task0-contracts-survey.md:304`). The hidden-dependency check mirrors the same rule (`work/10-next-plans/task0-contracts-survey.md:346`).

The cli/pipeline-side `pool.rs` boundary is clear enough for execution: M1-C extracts the embedding pool wrappers, while M1-B generalizes only the storage insert APIs they call (`work/10-next-plans/task0-contracts-survey.md:121`, `work/10-next-plans/task0-contracts-survey.md:126`, `work/10-next-plans/task0-contracts-survey.md:269`, `work/10-next-plans/task0-contracts-survey.md:300`). I do not see a remaining shared-file collision or dependency ambiguity that should block the first wave.

VERDICT: GO
