## Findings

No BLOCKER/WARN/NIT findings.

## Verification Notes

The r7 blocker is resolved in the current `deploy.sh`. The merge now passes only the managed key-name list through `awk -v` at [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:369). Credential values are read from the staged file as input data at [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:372) through [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:375), captured with `substr($0, length(k) + 2)`, and emitted with `print` at [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:379) and [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:382). I do not see any remaining path where managed credential values are sent through `awk -v`, sed replacement text, `printf %b`, or another escape-interpreting replacement channel during the remote merge.

I reproduced the merge locally with staged values containing literal backslash sequences such as `\n`, `\t`, and `\b`; the merged output preserved those bytes exactly while replacing provided managed keys, appending newly provided managed keys, and preserving omitted managed keys plus operator-added lines. The unchanged merge path still compares the merged candidate against the live file before installing it, so an idempotent re-run reports `kept existing $R_ENV` at [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:384).

The merge contract remains intact. Fresh hosts install the staged `producer.env` as root-owned mode 0600 at [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:365) through [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:366). Existing hosts merge-upsert only non-empty staged managed values, because the local staging block emits each managed key only when its deploy-time variable is non-empty at [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:202) through [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:205). Omitted managed keys and extra operator lines are preserved by the existing-file pass at [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:377) through [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:380), so I do not see a path that blanks an unprovided credential.

The safety and remote-execution invariants are still present: `set -euo pipefail` is enabled locally at [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:54) and in the remote install block at [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:276); the remote install heredoc escapes remote-side variables throughout the merge and metadata convergence block; `producer.env` is converged to `root:root` mode `0600` at [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:391) through [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:392); and Phase 6 still asserts exactly `600 root root` for the env file at [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:469) through [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:475). `--dry-run` still exits before remote staging and install at [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:246) through [deploy.sh](/home/pierre/Work/jurisearch/deploy.sh:252).

The four managed names still match the producer. `OPENROUTER_API_KEY` is the configured embedding `api_key_env` in [dist/update-server/config/producer.toml.example](/home/pierre/Work/jurisearch/dist/update-server/config/producer.toml.example:72). Production PISTE reads `PISTE_API_KEY`, `PISTE_OAUTH_CLIENT_ID`, and `PISTE_OAUTH_CLIENT_SECRET` as fallback credential names in [crates/jurisearch-official-api/src/config.rs](/home/pierre/Work/jurisearch/crates/jurisearch-official-api/src/config.rs:13), [crates/jurisearch-official-api/src/config.rs](/home/pierre/Work/jurisearch/crates/jurisearch-official-api/src/config.rs:18), and [crates/jurisearch-official-api/src/config.rs](/home/pierre/Work/jurisearch/crates/jurisearch-official-api/src/config.rs:28).

I ran `bash -n deploy.sh` and `shellcheck deploy.sh`; both exited 0. `bash -n` printed only the existing command-substitution heredoc warnings seen in the prior review.

VERDICT: GO
