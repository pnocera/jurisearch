# Review - Task 0 Contracts/API Survey r3

## Findings

### WARN - The M1-B storage-helper handoff is still incomplete for the real S5/S6 paths

r3 does assign the helpers called out in the r2 review to M1-B (`work/10-next-plans/task0-contracts-survey.md:83`, `work/10-next-plans/task0-contracts-survey.md:93`, `work/10-next-plans/task0-contracts-survey.md:94`, `work/10-next-plans/task0-contracts-survey.md:95`, `work/10-next-plans/task0-contracts-survey.md:96`, `work/10-next-plans/task0-contracts-survey.md:97`). However, the survey's enumerated list is still not complete against the actual `crates/jurisearch-cli` ingest/enrich/embed call paths, so M1-C would still have to edit storage or duplicate storage logic to ship the external-producer `DbClientSource` surface.

The missing tail is mostly zone-unit build/embed plus embedding insertion/readiness:

- `build_zone_units_payload` calls `load_derivable_decision_zones_json(&postgres, ...)` before `replace_zone_units_for_document` (`crates/jurisearch-cli/src/ingest/pipeline.rs:272`), but the r3 `zone_units.rs` handoff only lists `enrich_zone_candidates_json`, `replace_zone_units_for_document`, and `zone_retrieval_coverage_json` (`work/10-next-plans/task0-contracts-survey.md:95`, `work/10-next-plans/task0-contracts-survey.md:214`, `work/10-next-plans/task0-contracts-survey.md:232`). `load_derivable_decision_zones_json` is `ManagedPostgres`-typed and uses `execute_sql` (`crates/jurisearch-storage/src/zone_units.rs:228`, `crates/jurisearch-storage/src/zone_units.rs:229`, `crates/jurisearch-storage/src/zone_units.rs:251`).
- `embed_zone_units_payload` calls `load_zone_unit_embedding_inputs` (`crates/jurisearch-cli/src/ingest/pipeline.rs:378`, `crates/jurisearch-cli/src/ingest/pipeline.rs:412`), `embed_and_insert_zone_units_with_pool` (`crates/jurisearch-cli/src/ingest/pipeline.rs:394`, `crates/jurisearch-cli/src/ingest/pipeline.rs:423`), and `finalize_zone_dense_rebuild` (`crates/jurisearch-cli/src/ingest/pipeline.rs:444`). All are still `ManagedPostgres`-bound through storage/pool wrappers: `load_zone_unit_embedding_inputs` (`crates/jurisearch-storage/src/zone_units.rs:304`, `crates/jurisearch-storage/src/zone_units.rs:305`), `insert_zone_unit_embeddings` (`crates/jurisearch-storage/src/zone_units.rs:349`, `crates/jurisearch-storage/src/zone_units.rs:350`), `finalize_zone_dense_rebuild` (`crates/jurisearch-storage/src/zone_units.rs:501`, `crates/jurisearch-storage/src/zone_units.rs:502`), and the CLI pool wrapper itself (`crates/jurisearch-cli/src/embedding_runtime/pool.rs:361`, `crates/jurisearch-cli/src/embedding_runtime/pool.rs:362`, `crates/jurisearch-cli/src/embedding_runtime/pool.rs:387`).
- `embed_chunks_payload` calls `invalidate_cached_query_readiness(&postgres)` before embedding (`crates/jurisearch-cli/src/ingest/pipeline.rs:493`) and routes writes through `embed_and_insert_chunks_with_pool` (`crates/jurisearch-cli/src/ingest/pipeline.rs:539`, `crates/jurisearch-cli/src/ingest/pipeline.rs:574`). The listed `dense.rs` readers/finalizer are not enough: `invalidate_cached_query_readiness` is `ManagedPostgres`-typed (`crates/jurisearch-storage/src/ingest_accounting/readiness.rs:193`, `crates/jurisearch-storage/src/ingest_accounting/readiness.rs:194`), and `insert_chunk_embeddings` is also `ManagedPostgres`-typed (`crates/jurisearch-cli/src/embedding_runtime/pool.rs:324`, `crates/jurisearch-cli/src/embedding_runtime/pool.rs:325`, `crates/jurisearch-cli/src/embedding_runtime/pool.rs:352`, `crates/jurisearch-storage/src/projection/embeddings.rs:14`, `crates/jurisearch-storage/src/projection/embeddings.rs:15`).
- The legislation-citation collection path calls `load_archived_decisions_with_visa_json(&postgres, ...)` (`crates/jurisearch-cli/src/enrichment/legislation.rs:207`, `crates/jurisearch-cli/src/enrichment/legislation.rs:208`), but r3 only assigns `finalize_citation_occurrence_counts`, `load_pending_citation_resolutions_json`, and `legislation_citations_coverage_json` (`work/10-next-plans/task0-contracts-survey.md:96`, `work/10-next-plans/task0-contracts-survey.md:215`, `work/10-next-plans/task0-contracts-survey.md:234`). `load_archived_decisions_with_visa_json` is `ManagedPostgres`-typed and uses `execute_sql` (`crates/jurisearch-storage/src/legislation_citations.rs:15`, `crates/jurisearch-storage/src/legislation_citations.rs:16`, `crates/jurisearch-storage/src/legislation_citations.rs:24`).

Actionable fix: expand the M1-B storage-helper generalization deliverable everywhere it appears (§1 sub-table, §3 primary files, §4 ownership/handoff, and §6 hidden-dependency check) to include at least `crates/jurisearch-storage/src/zone_units.rs::{load_derivable_decision_zones_json, load_zone_unit_embedding_inputs, insert_zone_unit_embeddings, finalize_zone_dense_rebuild}`, `crates/jurisearch-storage/src/projection/embeddings.rs::insert_chunk_embeddings`, `crates/jurisearch-storage/src/ingest_accounting/readiness.rs::invalidate_cached_query_readiness`, and `crates/jurisearch-storage/src/legislation_citations.rs::load_archived_decisions_with_visa_json`, with client-source / `*_with_client` variants kept behind thin `ManagedPostgres` wrappers. Then have M1-C consume those generalized APIs only.

### WARN - The M1-C dependency table still permits starting after S1 only

The narrative handoff now correctly says the storage-helper generalization must be reviewed and merged before M1-C begins (`work/10-next-plans/task0-contracts-survey.md:237`, `work/10-next-plans/task0-contracts-survey.md:238`, `work/10-next-plans/task0-contracts-survey.md:240`, `work/10-next-plans/task0-contracts-survey.md:241`). But the branch split table still lists M1-C as depending only on `C0 + S1 from M1-B` (`work/10-next-plans/task0-contracts-survey.md:148`). An orchestrator following the table can start M1-C after the trait lands but before the storage helpers land, which contradicts the r3 fix and reopens the shared-file collision.

Actionable fix: change the M1-C dependency cell to `C0 + S1 from M1-B + M1-B storage-helper generalization`, or split M1-B into named sub-deliverables and make M1-C depend on both S1 and the complete helper-generalization deliverable.

## Re-verification Notes

The previously reviewed fixes for the root `Cargo.toml` C0 scaffolding, the S7 client-factory seam, producer config parser ownership, and `ErrorObject` ownership still read consistently. I did not find a new import-cycle problem beyond the unresolved storage-helper tail above. The M1 split is not safe to execute yet because the M1-B-owned helper list is incomplete and the dependency table still allows M1-C to start too early.

VERDICT: FIXES_REQUIRED
