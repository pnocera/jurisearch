## Findings

No BLOCKER/WARN/NIT findings.

## Verification Notes

The r3 watchdog false-green is fixed. In `crates/jurisearch-deploy/scripts/single-host-demo-acceptance.sh:105-106`, the authorized Tier-2 watchdog leg now runs `jurisearchctl site watchdog` and, on any non-zero exit, executes the same failure aggregation shape used by the other required legs: it prints `[FAIL] site watchdog ...` and sets `RC=1`. The script then reports and exits that aggregate value at `crates/jurisearch-deploy/scripts/single-host-demo-acceptance.sh:111-113`, so a watchdog `ALERT` process exit can no longer be swallowed into an overall green run.

The other Tier-2 legs were not weakened. `demo up`, `demo url`, `demo smoke`, `site smoke`, and `demo down` still aggregate non-zero exits into `RC=1` at `crates/jurisearch-deploy/scripts/single-host-demo-acceptance.sh:87-99` and `crates/jurisearch-deploy/scripts/single-host-demo-acceptance.sh:108-109`. The only intentional skips remain explicit and reasoned: unauthorized Tier-2 at `crates/jurisearch-deploy/scripts/single-host-demo-acceptance.sh:59-65`, authorized-without-config at `crates/jurisearch-deploy/scripts/single-host-demo-acceptance.sh:67-70`, absent committed fixture bytes for fixture demo legs at `crates/jurisearch-deploy/scripts/single-host-demo-acceptance.sh:80-85`, and operated real-id site smoke when `--fetch-id` is omitted at `crates/jurisearch-deploy/scripts/single-host-demo-acceptance.sh:97-103`.

The watchdog command itself still returns non-zero on alert states. `jurisearchctl` computes `alert` from any corpus whose `WatchdogStatus::is_alert()` is true at `crates/jurisearch-deploy/src/bin/jurisearchctl.rs:718-724`, emits the report, and returns `exit(u8::from(alert))` at `crates/jurisearch-deploy/src/bin/jurisearchctl.rs:739-740`. The alert states remain only `StalledCursor` and `AheadOfHead` at `crates/jurisearch-deploy/src/ops/watchdog.rs:90-96`.

The r1/r2 accepted watchdog behavior still holds. `classify_watchdog` distinguishes `applied == head` as `NoNewPackages`, reports `applied > head` as `AheadOfHead`, and fails closed to `StalledCursor` for a behind cursor with stale or unknown age at `crates/jurisearch-deploy/src/ops/watchdog.rs:131-144`. The live watchdog path reads verifier/manifest/cursor/status only at `crates/jurisearch-deploy/src/ops/watchdog.rs:197-228` and does not call `run_catchup` or any apply path. The PostgreSQL `timestamptz::text` parser accepts the real space-separated/numeric-offset formats at `crates/jurisearch-deploy/src/ops/watchdog.rs:270-298`, with coverage for `2026-06-29 12:00:00+00`, fractional seconds, RFC3339, and non-zero offsets at `crates/jurisearch-deploy/src/ops/watchdog.rs:416-459`.

The strict negative-leg contracts are preserved. Missing-id fetch passes only on an existing empty `documents` array or served `NoResults`, and fails leaked/unrelated documents, non-contract success shapes, other served errors, and transport errors at `crates/jurisearch-deploy/src/ops/smoke.rs:291-299` and `crates/jurisearch-deploy/src/ops/smoke.rs:362-404`. Bad-query search passes only on served `BadInput` or an existing empty `candidates` array, and fails candidates, missing/non-array `candidates`, unrelated served errors, and transport errors at `crates/jurisearch-deploy/src/ops/smoke.rs:331-339` and `crates/jurisearch-deploy/src/ops/smoke.rs:406-448`.

The `demo up` hard-fail path remains intact. Demo mode still preflights committed fixture artifacts at `crates/jurisearch-deploy/src/bin/jurisearchctl.rs:404-411`, performs bounded synchronous fixture catch-up before readiness/start at `crates/jurisearch-deploy/src/bin/jurisearchctl.rs:494-516`, and returns an error before the readiness gate or `UNIT_SITE` start when `demo_catchup_blocking_reason` reports non-green at `crates/jurisearch-deploy/src/bin/jurisearchctl.rs:501-510` and `crates/jurisearch-deploy/src/bin/jurisearchctl.rs:518-533`. Non-demo `site install` still calls `run_install(&args, false)` at `crates/jurisearch-deploy/src/bin/jurisearchctl.rs:258`, so it does not enter that demo-only block.

The Tier-1 loop remains corrected. The acceptance script invokes one `cargo test -p jurisearch-deploy --quiet "$module"` per module filter and aggregates failures through `TIER1_RC` into `RC=1` at `crates/jurisearch-deploy/scripts/single-host-demo-acceptance.sh:41-55`, avoiding the prior invalid multi-filter cargo invocation while preserving failure propagation.

The fixture deferral remains honest and guarded. `ensure_published_artifacts` fails fast for each configured corpus whose `sync.source_root/<corpus>/manifest.json` is absent and names both the missing path and generator at `crates/jurisearch-deploy/src/ops/fixture.rs:68-98`; `demo smoke` calls the same guard at `crates/jurisearch-deploy/src/bin/jurisearchctl.rs:677-685`. The fixture README explicitly states the committed bytes are deferred, the live fixture demo is unavailable until generated and committed, and the acceptance script skips those fixture demo legs with a recorded reason at `crates/jurisearch-deploy/fixtures/README.md:37-55`.

Scope and dependency cone remain acceptable. The diff is confined to `jurisearch-deploy` implementation, its fixture/readme/script, lockfile/Cargo metadata, and the two-host runbook; `jurisearch-client` is added only as a `jurisearch-deploy` dependency for the operator smoke runner at `crates/jurisearch-deploy/Cargo.toml:29-35`, so the thin-client crate's own dependency cone is not widened.

I ran `bash -n crates/jurisearch-deploy/scripts/single-host-demo-acceptance.sh` and `git diff --check main`; both passed. I did not run `cargo test` because that would write build artifacts outside the requested review file.

VERDICT: GO
