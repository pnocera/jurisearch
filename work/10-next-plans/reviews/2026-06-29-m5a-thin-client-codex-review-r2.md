WARN: `doctor` still makes an env-selected endpoint depend on config-home availability. In `run_doctor`, the advisory handling for “no config” is only applied when `resolve_config_path()` returns `Some(path)` and `load_config_at(&path)` returns `Ok(None)`; if neither `$XDG_CONFIG_HOME` nor an absolute `$HOME` is available, the `None` arm unconditionally reports `[FAIL]` and flips `healthy` to false before endpoint resolution/handshake run. That means `env -u HOME -u XDG_CONFIG_HOME JURISEARCH_SITE_URL=tcp://healthy:port jurisearch-client doctor` exits 2 even though the endpoint selector is present and the endpoint can resolve/probe. This contradicts the stated fix that doctor should treat the missing-config branch as advisory whenever `endpoint_selector_present` is true and fail only when nothing resolves. The existing acceptance test sets `XDG_CONFIG_HOME`, so it does not cover this no-config-directory variant. See [crates/jurisearch-client/src/main.rs](/home/pierre/Work/jurisearch-worktrees/m5a-thin-client/crates/jurisearch-client/src/main.rs:121) and [crates/jurisearch-client/src/main.rs](/home/pierre/Work/jurisearch-worktrees/m5a-thin-client/crates/jurisearch-client/src/main.rs:148).

WARN: `resolve_endpoint_with_config` still treats a present but non-Unicode `$JURISEARCH_SITE_URL` as absent, so one malformed-env class can fall through to the configured URL in doctor. The shared selector correctly uses `var_os` presence semantics, but endpoint resolution uses `std::env::var("JURISEARCH_SITE_URL")` and only parses the env URL on `Ok(url)`. On Unix, a present non-UTF-8 value returns `Err(NotUnicode)` here; in the forward path the config has already been skipped, so this degrades to a misleading “no site service selected” error, while in `doctor` a valid loaded config is passed to `resolve_endpoint_with_config` and can be used despite the present malformed env var. That misses the requirement that a present-but-malformed env var remains an endpoint error with no fall-through. See [crates/jurisearch-client/src/lib.rs](/home/pierre/Work/jurisearch-worktrees/m5a-thin-client/crates/jurisearch-client/src/lib.rs:158) and [crates/jurisearch-client/src/main.rs](/home/pierre/Work/jurisearch-worktrees/m5a-thin-client/crates/jurisearch-client/src/main.rs:158).

The prior blocker for ordinary explicit/env URL strings is fixed: `run_forward` calls `endpoint_selector_present` before loading config, so explicit `--server` and valid `$JURISEARCH_SITE_URL` forwards are no longer hostage to a malformed fallback file. The new shipped-binary tests at [crates/jurisearch-client/tests/configure_doctor.rs](/home/pierre/Work/jurisearch-worktrees/m5a-thin-client/crates/jurisearch-client/tests/configure_doctor.rs:132), [crates/jurisearch-client/tests/configure_doctor.rs](/home/pierre/Work/jurisearch-worktrees/m5a-thin-client/crates/jurisearch-client/tests/configure_doctor.rs:152), and [crates/jurisearch-client/tests/configure_doctor.rs](/home/pierre/Work/jurisearch-worktrees/m5a-thin-client/crates/jurisearch-client/tests/configure_doctor.rs:173) are meaningful regression tests for those paths.

The stale-temp permission issue is fixed for the reviewed path: `save_config_at` now uses `create_new(true)`, removes a stale temp on `AlreadyExists`, sets `0600` on the fresh temp before rename, and the regression test exercises a `0644` stale temp becoming a final `0600` config. See [crates/jurisearch-client/src/config.rs](/home/pierre/Work/jurisearch-worktrees/m5a-thin-client/crates/jurisearch-client/src/config.rs:107) and [crates/jurisearch-client/src/config.rs](/home/pierre/Work/jurisearch-worktrees/m5a-thin-client/crates/jurisearch-client/src/config.rs:179).

Verification run: `cargo test -p jurisearch-client` passed, including `tests/dependency_cone.rs`. `cargo tree -e normal --prefix none -p jurisearch-client` shows the added normal dependency surface is still limited to `serde`/`toml` plus their parser dependencies and does not pull in the forbidden heavy crates.

VERDICT: FIXES_REQUIRED
