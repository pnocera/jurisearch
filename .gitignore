# Secrets — never commit (DESIGN §14/§16: env / OS keyring only)
secrets.txt
secrets/
*.secret
*.secrets
.env
.env.*
*.pem
*.key

# Local index artifacts / embedded Postgres data dir
.codegraph/
index/
*.sqlite
*.parquet

# Repo-local generated release artifacts (./dist/)
/dist/

# Rust
/target
**/*.rs.bk

# Python (uv / venv — e.g. local-embed-tests)
.venv/
__pycache__/
*.pyc

# codex-review skill byproducts (keep only the .md review artifacts)
*.codex-log.txt
*.done
*.inprogress
*.lock
*.failed
